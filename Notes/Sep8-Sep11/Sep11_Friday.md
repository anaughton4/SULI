# Goals for Today
* Work on GHOSTS Profile
* Continue multi-level classification problem



# Notes



# What I did Today
* Got multi-level classificaiton done for feature engineering and local model
  * Need to do federated learning multi-classificaiton
* Got the RDP to work for GHOSTS
* Got the Sftp to work, although can't seem to command it to put the file in a specific location
  * Just drops file in the default origin \Users\Administrator
* SSH works, but need to find something for it to do
  * Trying to get it to add a line in one of the files (wsinfo) in the nera.zip folder.
  * Problem is that I need to extract the .zip folder first and then go into it which makes it more complicated.
  * Regardless, the SSH handler is operating correctly and actually executing.
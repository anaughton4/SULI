# Goals for Today
* Review other SULI poster board.
* Fix c code xapps for multiple slice configuration.
* Collect data for each scenario



# Notes
* Tried getting xapps to collect data on the slice level info
  * Got it to work, but it doesn't specifically say what IDs correspond to which slice.
  * I have to manually get that info by running each UE on each slice one at a time and seeing the IDs that are associated with that slice.
  * Then at the end, I have to merge these slice tables with the other xapp tables in order to group the metrics into each slice.



# What I did Today
# Goals for Today
* Look over real data
* Write paper and presentation



# Notes
* Real data can come from nwdaf, RIC, and grid plane.  All three can give us numeric data.
  * Numeric data is trained via an auto encoder or some similar deep neural network with time series forceasting
  * Can gather categorical variable (such as the direct logs) and train another model with that.
    * Use deep learning LLM, vectorization, or even xgboost/random forest models for this data.



# What I did Today
* Looked into real data:
  * Tried to figure out a structure where everytime I re-build the simulation, it deletes and re-builds the db_dir folder in jupyterlab (couldn't figure it out).
* Wrote paper for a litte.  Moved everythig to overleaf.
* Tried to gather data from the nwdaf.